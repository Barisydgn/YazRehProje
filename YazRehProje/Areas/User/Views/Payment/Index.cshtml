@model PaymentViewModel
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/User/Views/Shared/_UserLayout.cshtml";
}



<!-- Ödeme bağlantısını başlatmak için bir düğme -->






  @*  <div class="mb-2 col-md-6 mx-auto">
        <label for="exampleInputEmail1" class="form-label">Randevu Id</label>
        <input type="text" id="appointmentId" class="form-control" />

    </div>*@

    @* <input type="text" id="appointmentId" class="form-control" />*@
 @*   <div style="text-align:center;">
        <button type="submit" class="btn btn-primary text-center" id="start-payment"><i class="fa fa-check" aria-hidden="true"></i>Ödemeyi Yap</button>
    </div>*@



  

@*<input type="text" id="appointmentId" />

<button type="submit">>Ödemeyi Yap</button>
*@
@*@section scripts
    {
    <script src="https://js.stripe.com/v3/"></script>
    <script>
        $(document).ready(function () {
            $("#start-payment").click(function () {
                let appointmentId = $("#appointmentId").val();

                // AJAX isteği ile Checkout Session ID'sini al
                $.ajax({
                    url: '/User/Payment/CreateCheckoutSession',
                    method: 'POST',
                    contentType: 'application/json',
                    data: JSON.stringify({
                        AppointmentId: appointmentId
                    }),
                    success: function (response) {
                        // Başarıyla alındıysa yönlendirme yap
                          //var paymentResponse = @Html.Raw(Json.Serialize( ViewData["PaymentResponse"]));
                        window.location.href = response.url
                    },
                    error: function (xhr, status, error) {
                        // Hata durumunda burada işlem yapabilirsiniz
                        console.error(error);
                    }
                });
            });
        });
    </script>
}*@

    
<div class="mb-2 col-md-6 mx-auto">
    <label for="appointmentId" class="form-label">Randevu Id</label>
    <input type="text" id="appointmentId" class="form-control"  />
</div>

<div style="text-align:center;">
    <button type="submit" class="btn btn-primary text-center" id="start-payment"><i class="fa fa-check" aria-hidden="true"></i>Ödemeyi Yap</button>
</div>



@section scripts
    {
    <script src="https://js.stripe.com/v3/"></script>
    <script>
        $(document).ready(function () {
            $("#start-payment").click(function () {
                let appointmentId = $("#appointmentId").val();

                // Kullanıcının girdiği appointmentId'yi kontrol et
                if (!appointmentId) {
                    alert("Lütfen bir Randevu ID'si girin.");
                    return;
                }

                // AJAX isteği ile Checkout Session ID'sini al
                $.ajax({
                    url: '/User/Payment/CreateCheckoutSession',
                    method: 'POST',
                    contentType: 'application/json',
                    data: JSON.stringify({
                        AppointmentId: appointmentId
                    }),
                    success: function (response) {
                        // Başarıyla alındıysa yönlendirme yap
                        window.location.href = response.url;
                    },
                    error: function (xhr, status, error) {
                        // Hata durumunda burada işlem yapabilirsiniz
                        console.error(error);
                    }
                });
            });
        });
    </script>
}


@*<form id="paymentForm" method="post">
    <div class="mb-2 col-md-6 mx-auto">
        <label for="appointmentId" class="form-label">Randevu Id</label>
        <input type="text" id="appointmentId" class="form-control" />
    </div>

    <div style="text-align:center;">
        <button type="button" class="btn btn-primary text-center" id="start-payment"><i class="fa fa-check" aria-hidden="true"></i>Ödemeyi Yap</button>
    </div>
</form>

@section scripts
    {
    <script src="https://js.stripe.com/v3/"></script>
    <script>
        $(document).ready(function () {
            $("#start-payment").click(function () {
                let appointmentId = $("#appointmentId").val();

                // Kullanıcının girdiği appointmentId'yi kontrol et
                if (!appointmentId) {
                    alert("Lütfen bir Randevu ID'si girin.");
                    return;
                }

                // Form elemanını göndermek için bir FormData nesnesi oluştur
                var formData = new FormData();
                formData.append("AppointmentId", appointmentId);

                // AJAX isteği ile Checkout Session ID'sini al
                $.ajax({
                    url: '/User/Payment/CreateCheckoutSession',
                    method: 'POST',
                    contentType: false,
                    processData: false,
                    data: formData,
                    success: function (response) {
                        // Başarıyla alındıysa yönlendirme yap
                        window.location.href = response.url;
                    },
                    error: function (xhr, status, error) {
                        // Hata durumunda burada işlem yapabilirsiniz
                        console.error(error);
                    }
                });
            });
        });
    </script>
}
*@